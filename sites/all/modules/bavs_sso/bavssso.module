<?php

/**
 * @file
 * An example implementation of OpenID Connect client.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function bavssso_ctools_plugin_directory($module, $plugin) {
  if ($module == 'openid_connect') {
    return 'plugins/' . $plugin;
  }
}
